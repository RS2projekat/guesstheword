angular.module("myControllers", []).controller("LoginController", [
    "$scope", "xs", function ($scope, xs) {
        var controller = xs.controller("Login");
        controller.on("BRANISLAVINAFUNKCIJA", function (model) {
            //$scope
        })
    }
]);